{
  "name": "livetranslate-gemini-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny proxy that holds the shared Gemini API key server-side, so the mobile app never ships a secret that every install would share.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "qrcode-terminal": "^0.12.0"
  }
}
